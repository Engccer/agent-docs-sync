# 줄바꿈 정규화: repo에는 항상 LF로 저장하고, 체크아웃 시 파일 유형별로 지정한다.
* text=auto

# 셸 스크립트는 어느 OS에서 체크아웃하든 LF를 유지한다(CRLF면 shebang `#!/usr/bin/env bash`가 깨진다).
*.sh text eol=lf

# Windows 전용 스크립트는 CRLF로 체크아웃한다.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
